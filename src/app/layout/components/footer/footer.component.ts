import { Component } from '@angular/core';

@Component({
  selector: 'app-footer',// The selector that will be used to render the component
  templateUrl: './footer.component.html', // The path to the HTML template
  styleUrls: ['./footer.component.css'] // The paths to the CSS stylesheets
})
export class FooterComponent {
  // Constructor for the ModalFIleViewComponent
}
